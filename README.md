# Simulation for the MochiSwarm platform
# Open model trainer to see how the models are being trained 
# the mppi controller is a work in progress

`pip install -r requirements.txt`
`python -m src.simulation`
